# Lispの仕様

## データ型
AtomとCons cell (Pair)のみとする．
- Atomは文字列，シンボル，T, nilなど，これ以上分割できない基本的なデータの単位である
- Cons cellは，二つの値のペアである． `(A . B)`と表記し，`.`で二つの要素を区切る．

## シンボル
- 真値: `T`
- 偽または空Cons cell `()`を表す: `nil`
  * ここで，`nil`はAtomではなく空のCons cellである．

## 関数
関数は，関数`f`があったとき，`(f A)`などと書いて呼び出す．
以下の5つの関数だけを実装する．
- `atom`: 引数を1つ受け取り，その値が`Atom`ならば`T`を返す．
- `eq`: 2つのAtomを受け取り，それらが等しければ`T`を返す．
- `car`: 1つのCons cellを受け取り，Cons cellの左側の要素を出力する．
- `cdr`: 1つのCons cellを受け取り，Cons cellの右側の要素を出力する．
- `cons`: 2つの引数を受け取り，その順番でCons cellを作って出力する．

## 特殊形式
以下の特殊形式を実装する．
- `cond`: 第一条件を評価して，`T`ならば結果を評価する．`nil`ならば次の条件... 最後にelse文に相当するデフォルトの処理を書く．
- `quote`: 引数を評価せずにそのまま扱う
- `lambda`: 無名関数を構成する．
- `define`: `(define x A)`として，`x`を評価すると`A`が返ってくる．

## 構文糖衣
- `(A B C)`と表記して，`(A . (B . (C . nil)))`と展開する．
- `'(A B)`と表記したら，`(quote (A B))`と展開する

## 言語の拡張
- (import foolib)のように外部のライブラリを使用できるようにする
- Lisp上の関数が，内部での実行はRustプログラムが走るようにすることができるようにする
  - Lispの関数名とRustでの実行を紐づけるしくみづくり